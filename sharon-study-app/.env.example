# Sharon Study 环境配置
# 复制为 .env.local (本地开发) 或 .env.production (服务器生产)

# 数据库路径 (相对于项目根目录 sharon-study-app/)
# 本地开发: data/test.db
# 生产环境: /data/sharon-study/production.db (绝对路径)
DB_PATH=data/test.db

# 服务端口
PORT=3000

# 运行环境
NODE_ENV=development
